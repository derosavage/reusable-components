[project]
name = "reusable-backend"
version = "0.1.0"
description = "Reusable backend components - Auth, Daraja, Analytics, Data Factory, LLM"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"
